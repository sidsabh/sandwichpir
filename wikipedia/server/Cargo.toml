[package]
name = "wiki-server"
version = "0.1.0"
edition = "2021"

[features]
cuda = ["pir_server/cuda"]
default = []

[dependencies]
pir_server = { path = "../../pir_server" }
log = "0.4"
env_logger = "0.11"
clap = { version = ">=4.5.0, <4.6.0", features = ["derive"] }
serde_json = "1.0"
